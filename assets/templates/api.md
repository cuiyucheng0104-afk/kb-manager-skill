# API 文档

> 最后更新：（待填写）

## 全局约定

- **Base URL**：（待补充，如 `http://localhost:8000/api`）
- **认证方式**：（待补充，如 `Authorization: Bearer <JWT>`）
- **内容类型**：`application/json`（如不同请修改）
- **错误格式**：（待补充，给出统一错误响应示例）

| 状态码 | 含义 |
|--------|------|
| 200 | 成功 |
| 400 | 参数错误 |
| 401 | 未认证 |
| 404 | 资源不存在 |
| 500 | 服务端错误 |

## 接口列表

| 方法 | 路径 | 说明 | 需认证 |
|------|------|------|--------|
| （待补充） | | | |

## 接口详情

每个接口按以下结构记录：

### GET /example/{id}

**说明**：（一句话说明接口做什么）

**路径参数**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | int | 是 | 资源 ID |

**请求示例**

```bash
curl -H "Authorization: Bearer <token>" http://localhost:8000/api/example/1
```

**响应示例**

```json
{
  "id": 1,
  "name": "示例"
}
```
